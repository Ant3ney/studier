using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WitchSlide : MonoBehaviour
{
    public int SlideNumber = 1;
    // Start is called before the first frame update
    void Start()
    {
        SlideNumber = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
